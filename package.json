{
  "name": "echoes-of-the-basin",
  "version": "2.0.0",
  "description": "Echoes of the Basin — late-1960s Amazon basin radio mystery",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "package:itch": "npm run build && node scripts/package-itch.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
